{
  "name": "dockerized-service-deployment",
  "version": "1.0.0",
  "description": "Node.js service with Basic Auth and CI/CD via GitHub Actions + GHCR",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "jest --forceExit --coverage"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  }
}
